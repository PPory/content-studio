# 所选工作台设计验收

日期：2026-09-07

## 视觉依据

用户选定原图：`C:/Users/Lenovo/.codex/generated_images/01a07090-da36-7882-b7f6-f2f60f1166cb/exec-e0ea220e-097d-496c-b588-027d2692e8a8.png`。

实现截图：`workbench/output/playwright/interview-topic-desktop.png`。
全屏并列对照：`workbench/output/playwright/design-comparison.jpg`，已实际打开检查。

原图与实现均为 1505 × 1045 像素；浏览器 CSS viewport 为 1505 × 1045，devicePixelRatio 为 1。对照图未裁剪或拉伸两张截图。原图为充实笔记与多轮讨论，实现为隔离测试中的短笔记、已有文章和两次讨论；内容与摘要空态差异不作像素一致声明。区域比例、导航、标题、页签、文档与持续聊天结构按所选图实现。

## 检查与修正

- 去掉选题页重复应用页头及外层容器，左侧思考 / 文章 / 资料切换，右侧讨论持续显示。
- AI 理解使用轻量可折叠区域，个人笔记独立呈现；工具移至聊天输入框上方。
- 修正手机聊天高度，390 × 844 下输入框完整可见，无横向溢出。
- 阅读页隐藏重复导航，正文首个同名标题只渲染一次，不修改原文。
- 首页概览、最近打开与最近阅读采用紧凑分区；资料分类入口放到列表前。

上述修改后的全屏对照未发现待处理 P0/P1/P2。重点查看顶部层级、左右列边界、摘要 / 笔记边界与底部输入区；全图足以辨认这些区域，不需要另行放大资产细节。

补充实际截图：`workbench/output/playwright/design-secondary-review.jpg` 包含首页、写作与手机讨论；阅读截图为 `workbench/output/playwright/interview-reading-desktop.png`，均已打开检查。

## 行为验证

隔离临时 XENHO_HOME 的浏览器测试通过：保存失败恢复、摘录取消、连续讨论、创建文章、正文保存、导出、资料切换不挤走聊天、返回笔记保留输入、阅读标题去重与位置恢复、手机输入框可见、首页离开保护。页面无未处理浏览器错误。

实际本地工作台 `http://127.0.0.1:5180/` 返回 HTTP 200。未调用收费模型进行视觉测试，也未向真实工作区写入测试业务数据。

## 有意保留的差异与验证范围

保留已有侧栏搜索和折叠入口、权限选择、模型选择与消息操作，因此侧栏较原图宽 16 像素，输入区比原图略高。真实内容长度决定笔记及聊天密度。没有引入原图中示例资料或虚构来源。未执行真实模型联网搜集；本次修改不涉及后端或模型调用契约。

final result: passed

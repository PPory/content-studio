# 表格与分隔线设计 QA

**对比目标**

- 参考图：D:\xwechat_files\wxid_yc8u5vzg6u0d12_9166\temp\InputTemp\a9fb36dd-52da-4983-8964-54421fd9e9d3.png（新增行悬停，1106 × 264）
- 参考图：D:\xwechat_files\wxid_yc8u5vzg6u0d12_9166\temp\InputTemp\a8251e67-003f-45c2-b50e-b2a3d8e4f941.png（新增列悬停，1030 × 207）
- 实现截图：workbench/output/playwright/table-add-row.png（741 × 187）
- 实现截图：workbench/output/playwright/table-add-column.png（741 × 187）
- 合并对比：workbench/output/playwright/table-reference-comparison.png（1600 × 720）
- 浏览器视口：1440 × 900 CSS px，deviceScaleFactor 1
- 状态：桌面端浅色主题；三行三列表格；分别悬停底部新增行热区、右侧新增列热区
- 归一化：合并图中各截图按原比例缩放到同宽面板，没有拉伸；比较表格内容区域，不比较编辑器外部页面

**Findings**

- 没有可执行的 P0、P1 或 P2 差异。
- 字体与排版：沿用编辑器现有正文和小型控件字体；单元格字号、行高和参考图密度一致。
- 间距与布局：三列等宽、三行等高；底部按钮覆盖表格内容宽度，右侧按钮覆盖表格高度，位置与参考图一致。
- 颜色与视觉标记：白色单元格、浅边框、浅灰悬停底色和低强调加号保持现有主题变量，明暗层级与参考图一致。
- 图片与资源：该组件不包含图片资源；没有以占位图替代参考中的可见资源。
- 文案与内容：新增入口使用无干扰的加号，辅助名称和提示分别说明“添加一行”“添加一列”。
- 参考图为空表格，实现截图填入测试内容并保留一个输入焦点描边；这是为了验证编辑与持久化的测试状态，不属于样式偏差。

**完整视图对比证据**

- 合并对比图同时包含两张参考图和两个浏览器实现状态。表格比例、边框密度、底部横向热区和右侧纵向热区均已对应。
- 浏览器中未出现横向溢出、控件遮挡或控制区常驻显示。

**局部对比证据**

- 新增行局部：按钮仅在表格下方悬停后可见，宽度与表格内容区一致。
- 新增列局部：按钮仅在表格右侧悬停后可见，高度与表格内容区一致。
- 拖拽局部未单独截图；真实浏览器已分别把第 3 行拖到第 2 行、把第 3 列拖到第 1 列，并验证单元格内容与保存后的 Markdown 顺序都发生对应变化。

**交互与控制台**

- 已验证：单元格编辑、底部新增行、右侧新增列、行拖拽重排、列拖拽重排、分隔线插入、刷新前保存。
- 页面异常与控制台错误：0。
- 分隔线前连续 3 个以上换行：未出现。

**Comparison history**

- 第一轮：参考图与实现合并对比后未发现 P0/P1/P2 问题，无需视觉返工。
- 交互回归中发现悬停样式存在 100ms 过渡；测试改为等待过渡完成后读取最终状态，产品实现未修改。

**Implementation Checklist**

- [x] 表格底部和右侧使用互不干扰的悬停热区
- [x] 行、列拖拽后更新可移植 Markdown
- [x] 新增行列后焦点进入新单元格
- [x] 分隔线不再叠加多余空行
- [x] 真实浏览器交互和控制台检查通过

**Follow-up Polish**

- 无阻塞项。

final result: passed
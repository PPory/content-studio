---
name: fact-check
description: 逐条核查正文中的数字、日期、人物、事件、专有名词、引语和绝对化判断，区分已证实、冲突、证据不足与不可核查。
---

# 事实核查

先用 `project_read` 提取可核查主张。项目已有证据用 `material_evidence`，公开事实使用联网搜索并阅读关键来源。

每条写清原表述、核查状态、依据、风险和建议改法。没有可靠来源时必须写“证据不足”；不凭模型记忆判定，不直接改正文。

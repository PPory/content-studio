{
  "name": "content-pipeline",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "migrate:supabase": "node scripts/migrate-d1-to-supabase.mjs",
    "tail": "wrangler tail",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}

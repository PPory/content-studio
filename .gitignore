node_modules/
.xenho/
tmp/
*.log
.DS_Store
Thumbs.db
desktop.ini

# tidy-project 隔离区：移进来的东西没有删，等确认后再清
.tidy-trash/

# 退役远端实现仅保留本机缓存，不再纳入仓库
worker/
supabase/

# 原始音频转写稿：产品讨论的素材来源，不是本仓库的内容。
# ⚠️ 仓库是公开的，而这些是别人视频的逐字全文——不要再被 `git add docs/` 扫进去。
docs/transcription_*.json
